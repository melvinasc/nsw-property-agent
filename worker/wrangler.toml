name = "nsw-property-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Free tier — no paid plan needed
[limits]
cpu_ms = 50

# Optional: set your custom domain after deploying
# [[routes]]
# pattern = "proxy.yourdomain.com/*"
# zone_name = "yourdomain.com"
