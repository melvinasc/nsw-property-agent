{
  "name": "nsw-property-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for NSW Property Agent",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
